node_modules
dist
pnpm-lock.yaml
packages/prisma/test/prisma/generated

# Hand-formatted with compact single-line objects; reformatting adds churn
# without improving readability.
**/package.json

# Archived planning documents — kept verbatim as a historical record.
docs/superpowers/plans

# Git-ignored scratch written by the subagent-driven-development workflow.
# Prettier scans it even though git does not, which makes `format:check` fail
# locally while still passing in CI's fresh checkout.
.superpowers

# Astro's generated type/content cache for the docs site.
website/.astro

# Generated by `pnpm --filter @node-media-library/core docs:api`. Formatting it
# would make CI's regenerate-and-diff freshness check fail permanently, since
# TypeDoc's own output is what the check compares against.
website/src/content/docs/api
