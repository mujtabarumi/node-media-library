{
  "name": "@node-media-library/examples",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Runnable versions of the code samples embedded in the documentation site. Never published; exists so a guide cannot drift from shipped behavior.",
  "engines": { "node": ">=20" },
  "scripts": { "test": "vitest run", "typecheck": "tsc --noEmit" },
  "dependencies": { "@node-media-library/core": "workspace:*" },
  "devDependencies": { "typescript": "^5.5.0", "vitest": "^2.0.0", "@types/node": "^20.0.0", "sharp": "^0.35.3" }
}
